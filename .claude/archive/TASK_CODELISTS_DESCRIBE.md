TASK_CODELISTS_DESCRIBE.md

Here I have code (part of a larger github repo) that helps generate the Error Correction Zoo website.  YAML data schemas in @schemas/ . You will assist the user for *minimal localized edits* to add some features the user needs.  Do NOT refactor code.  Do NOT edit code unless absolutely necessary.  FLAG issues for confirmation by the user instead of correcting them directly.  Task: The module `compile_codelist.js` (called by `render_codelist.js`) takes the eczoo db and compiles specific lists of code given by a set of conditions and predicates.  (1) Familiarize yourself with how the list compiling works.  Create `CODELISTS_HOWTO.md` with extensive documentation on how the list compilation works, i.e., how a user can specify a codelist with standard codelist data key/values and what their effect are in the compiled code list.  (2) Peek in the folder `../../eczoo_data/codelists/` (look for `**/*.yml` in there) to get examples of defined code lists.  (3) Find common patterns of how code lists are generated (e.g. descendants, property field, descendants with specific property set...?)  (4) Add a public function in `compile_codelist.js` named `describe_codelist()`.  The function should output an object with properties that provide (i) which "standard pattern" the list conforms to, if any (e.g. all descendants of a code), in a suitable machine-readable form, and (ii) valid FLM code that describes clearly, in words, (possibly with an enumeration list, if absolutely necessary), what the list describes (e.g. "All descendants of \ref{code:mycode}." or "All descendants of \ref{code:mycode} that are not cousins of \ref{code:othercodeid}").  Use a suitable standard formulation for the standard patterns.
